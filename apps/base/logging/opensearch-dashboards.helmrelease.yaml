apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: opensearch-dashboards
  namespace: logging
spec:
  chart:
    spec:
      chart: opensearch-dashboards
      sourceRef:
        kind: HelmRepository
        name: opensearch
        namespace: flux-system
      version: 2.27.1
  interval: 15m
  timeout: 5m
  releaseName: opensearch-dashboards

  values:
    ingress:
      enabled: true
      ingressClassName: traefik
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
        traefik.ingress.kubernetes.io/router.tls: "true"
        cert-manager.io/cluster-issuer: le-prod
      hosts:
        - host: ${opensearchIngressHost:logging.k8s.vincentdupain.com}
          paths:
            - path: /
              backend:
                serviceName: "opensearch-dashboards"
                servicePort: "http"
      # tls:
      #   - secretName: opensearch-dashboards-tls
      #     hosts:
      #       - ${opensearchIngressHost:logging.k8s.vincentdupain.com}

